# Life Assistant Admin

Vue 3 + Vite + TypeScript + Element Plus admin frontend for Life Assistant.

## Commands

```bash
pnpm install
pnpm dev
pnpm type-check
pnpm build
```

## API

The admin app uses the real backend API configured by Vite environment variables. Local mock API support has been removed.
